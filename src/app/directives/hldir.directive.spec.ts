import { HldirDirective } from './hldir.directive';

describe('HldirDirective', () => {
  it('should create an instance', () => {
    const directive = new HldirDirective();
    expect(directive).toBeTruthy();
  });
});
